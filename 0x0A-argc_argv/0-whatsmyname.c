#include <stdio.h>

/**
 * main - prints the name of the program
 * @argv: an array of strings containing the arguments
 *
 * Return: 0 on success
 */
int main(char *argv[])
{
        printf("%s\n", argv[0]);
        return (0);
}
